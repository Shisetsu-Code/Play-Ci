name: Capture corrected 3Oaks popups

on:
  pull_request:
    paths:
      - 'scripts/capture-corrected-popups.js'
      - '.github/workflows/capture-corrected-popups.yml'

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - run: node scripts/capture-corrected-popups.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: 3oaks-corrected-popups
          path: artifacts/layout-popups/
          retention-days: 1
