name: Capture remaining 3Oaks layouts

on:
  pull_request:

permissions:
  contents: read

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - run: node scripts/capture-corrected-popups.js
      - run: node scripts/capture-kendoo-popups.js
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: remaining-layout-captures
          path: |
            artifacts/layout-popups/
            artifacts/kendoo-popups/
          retention-days: 1
